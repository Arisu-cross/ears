# ears 云端部署指南 · 给不是工程师的你

把 ears 从「只能在自己电脑上跑的小工具」，变成一个 7×24 小时在线、手机随时能用、还能接给你的 AI 的服务。

> **写给谁**：不是工程师的人
> **要多久**：约 40–90 分钟
> **要花多少钱**：每月大约一杯咖啡

---

## 先说清楚：你会得到什么

ears 做的事是——你说话，它不只记下你**说了什么**，还分析你**是怎么说的**：音量、停顿、语速、音高起伏。然后拿这些跟「你平时的样子」比，告诉你的 AI：她这次说得比平时轻、停顿比平时长。

本地跑（原项目 README 那条路）已经能用，但每次都得开电脑、开终端。**放到云端之后**：手机浏览器打开一个网址就能录，关掉也还在，还能让你的 AI 直接调用。

> ### 💡 期待管理
>
> **前 8 句话它在「认识你」，不是在偷懒。**
>
> ears 比的是「你自己的平时」而不是大众平均值，所以它得先听够 8 条录音建立你的基线。**第 9 条起**才会开始出现「和平时比：音量明显偏低」这类判断。前 8 条只有内容和粗略语气——这是设计如此，不是坏了。

---

## 开始前，先备齐这四样

- [ ] **一个 GitHub 账号** — 免费。用来存放代码。
- [ ] **一个 Groq 账号** — 免费，不用绑卡。它负责把你的语音转成文字，并判断语气。注册地址 `console.groq.com`
- [ ] **一个云平台账号** — 本文以 Zeabur 为例（对新手友好、支持中文）。Railway、Render 之类思路完全一样。这一项要花钱，通常每月几美元。
- [ ] **一台能打开网页的设备** — 手机也行。只有「可选进阶」那节需要用到电脑。

---

# 第一部分：把代码改成「能上云」的样子

原项目是**为本地设计的**——它默认只在你自己电脑上跑，所以有两件事没做，而这两件事一旦放到公网上就会要命。你必须先补上。

> ### ⚠️ 最重要的坑 · 接口是不设防的
>
> 原版 ears 的接口**没有任何密码**。在自己电脑上无所谓，但一放到公网，**任何知道你网址的人都能读走你所有的录音记录、甚至删掉它们**。原作者在 README 里也提醒过这点。
>
> 所以下面第 2 步不是可选项，**是必做项**。

## 步骤 1 · Fork 一份代码到自己名下

打开原项目页面，点右上角 **Fork**。这样你就有了一份可以随便改的副本，改坏了也不影响原作者。

## 步骤 2 · 给接口加一把锁

在 GitHub 网页上直接编辑 `server.py`（点文件右上角的铅笔图标就能改，不用装任何东西）。

**(1)** 找到 `KEEP_AUDIO = ...` 那一行，**在它下面**加一行：

```python
EARS_TOKEN = _env("EARS_TOKEN")
```

**(2)** 找到 `app = FastAPI()` 那一行，**在它下面**粘贴这一整段：

```python
_OPEN_PATHS = {"/health"}

@app.middleware("http")
async def token_gate(request, call_next):
    # 没带正确暗号的请求，一律挡回去
    if EARS_TOKEN and request.url.path not in _OPEN_PATHS:
        got = request.headers.get("x-token") \
              or request.query_params.get("token") or ""
        if not hmac.compare_digest(got, EARS_TOKEN):
            return JSONResponse({"error": "unauthorized"}, status_code=401)
    return await call_next(request)

@app.get("/health")
def health():
    return {"ok": True, "asr": bool(GROQ_KEY), "auth": bool(EARS_TOKEN)}
```

**(3)** 翻到文件**最上面**，在那一堆 `import` 里加一行：

```python
import hmac
```

**(4)** 顺便改一下网页文件 `static/index.html`，让它能带着暗号访问。找到 `function stop() {`，**在它上面**加：

```javascript
const api = (p) => {
  const t = new URLSearchParams(location.search).get("token");
  return t ? p + "?token=" + encodeURIComponent(t) : p;
};
```

然后把同一个文件里的这两处：

| 原来 | 改成 |
|---|---|
| `fetch("/api/listen"` | `fetch(api("/api/listen")` |
| `fetch("/api/forget"` | `fetch(api("/api/forget")` |

## 步骤 3 · 新建一个 Dockerfile

这个文件告诉云平台「怎么把你的代码跑起来」。在 GitHub 上点 **Add file → Create new file**，文件名就叫 `Dockerfile`（没有后缀），内容整段复制：

```dockerfile
FROM python:3.11-slim

# ffmpeg 负责转换音频格式，libsndfile1 是分析音频要用的
RUN apt-get update && apt-get install -y --no-install-recommends \
        ffmpeg libsndfile1 \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY . ./

# 录音基线和记录都存这里，等下要把「持久卷」挂到这个位置
ENV DATA=/app/data
RUN mkdir -p /app/data

EXPOSE 8020
CMD ["sh", "-c", "uvicorn server:app --host 0.0.0.0 --port ${PORT:-8020}"]
```

## 步骤 4 · 把笨重的可选功能先关掉

编辑 `requirements.txt`，把 `onnxruntime` 那一行**前面加个 `#`** 注释掉：

```
# onnxruntime
```

它是「声纹锁」和「环境音识别」用的，体积大、构建慢，而且还需要另外下载模型文件才能真正工作。**第一次部署先不要装**——转写和语气分析完全不受影响。等跑通了想加再说（见文末进阶）。

---

# 第二部分：部署上线

## 步骤 5 · 先领一把 Groq 钥匙

打开 `console.groq.com` 注册登录，进 **API Keys**（或直接访问 `console.groq.com/keys`），点 **Create API Key**，名字随便起。

> ### ⚠️ 注意
>
> 生成的那串 `gsk_` 开头的字符**只显示这一次**，关掉就再也看不到了。马上复制存好。丢了不要紧，删掉重建一个就行。

## 步骤 6 · 在云平台上创建服务

登录 Zeabur → 新建项目 → 添加服务 → 选**从 GitHub 部署** → 选你刚 Fork 的那个仓库。

平台会自动发现你写的 Dockerfile 并开始构建。**第一次构建要 5～15 分钟**（它在装 ffmpeg 和一堆音频库），慢是正常的，泡杯茶。

> ### ⚠️ 如果构建失败了
>
> 有些账号或套餐**不允许在平台上直接构建**，日志里会出现类似 `not allowed to deploy` 的提示。这跟你的代码没关系。两个办法：
>
> 1. 去账单页面看看套餐是否需要升级；
> 2. 走「自己构建镜像」的路线（见文末进阶那节）。

## 步骤 7 · 填环境变量

在服务的 **Variables / 环境变量** 页面，加这几条：

| 名字 | 填什么 |
|---|---|
| `GROQ_API_KEY` | 步骤 5 拿到的那串 `gsk_...` |
| `EARS_TOKEN` | **你自己随便编一串长密码**，越乱越好，比如 `xK9-mQ2vB7nR4tL8wZ`。这就是你接口的锁。 |
| `ASR_LANG` | `zh`（说中文的话） |
| `KEEP_AUDIO` | `0` — 分析完就删掉录音，不留存 |
| `OWNER_NAME` | 你的名字或昵称，随便写 |
| `PORT` | `8020` — **别漏了这条**，理由见下 |

> ### ⚠️ 很容易卡住的坑 · 502 错误
>
> 有些平台会自己往容器里塞一个**随机端口号**。结果就是：程序在 A 端口上听着，平台却往 B 端口送流量，两边对不上，你打开网址只会看到 **502 Bad Gateway**，而且日志里一切正常，特别难猜。
>
> 解决办法就是上面那条：**明确写死 `PORT=8020`**，跟 Dockerfile 里的 `EXPOSE 8020` 对齐。

## 步骤 8 · 挂一个「持久卷」

在服务设置里找到 **Volumes / 存储卷**，添加一个，挂载路径填：

```
/app/data
```

> ### ⚠️ 不做的后果
>
> 云上的容器**每次重启都会恢复出厂设置**。不挂这个卷，你辛辛苦苦录的 8 句基线、所有历史记录，**重启一次就全没了**，还得从头再来。挂上它，数据才真正存得住。

## 步骤 9 · 绑定网址，然后验收

在 **Domains / 域名** 里生成一个免费网址，比如 `你起的名字.zeabur.app`。

先访问 **`你的网址/health`**。看到这样一行就说明活了：

```json
{"ok":true,"asr":true,"auth":true}
```

三个都是 `true` 才算齐活：`asr` 是 Groq 钥匙配好了，`auth` 是锁装上了。

然后用**带暗号的完整地址**打开录音页：

```
https://你的网址/?token=你设的EARS_TOKEN
```

> ### ✅ 成了
>
> 按住按钮说一句话，松开。几秒后出现一张卡片，写着你说了什么、语气标签是什么。**把这个完整网址存成手机书签**，以后点开就能用。

---

# 接给你自己的 AI

ears 分析完的结果，有三种拿法，按省事程度排：

| 方式 | 怎么用 | 适合 |
|---|---|---|
| **复制粘贴** | 录完点结果卡上的复制键，粘给 ChatGPT / Claude 网页版 | 不想写代码，或者用的是封闭的 App |
| **让 AI 自己读** | 调 `GET /api/recent?n=20` 拿最近的记录 | 你的 AI 能上网抓东西 |
| **直接送音频** | 把录音 POST 到 `/api/listen`，拿回文字＋语气 | 你自己有一层能收语音的程序（比如接了 Telegram 机器人） |

所有请求都要带上暗号，二选一：请求头里加 `X-Token: 你的EARS_TOKEN`，或者网址后面挂 `?token=你的EARS_TOKEN`。

> ### 💡 把结果喂给 AI 时的关键一条
>
> 原作者特别强调过：**把语气分析结果绑在「具体那条消息」上，不要做成一直飘在那儿的全局设定。**
>
> 比如写成 `[语音] 我没事（语气：低落，音量比平时明显低）` 跟着那句话一起给它。
>
> 做成全局的话，AI 会拿一小时前的情绪去理解你现在说的话，越理解越偏。

---

# 踩过的坑，一次性列给你

### 录音页打不开麦克风 / 提示要权限

浏览器只在 **HTTPS** 或 `localhost` 下才给麦克风权限。云平台送的网址默认就是 HTTPS，没问题；但如果你自己绑了域名却没配证书，就会卡在这里。另外 iPhone 上要用 Safari，且首次会弹权限询问，别手滑点了拒绝。

### 它一直不说「和平时比怎么样」

前 8 条在建基线，第 9 条起才有。另外，如果你每次都用**几乎一模一样的语气**说话，它也可能什么都不报——因为确实没有偏差。这是对的行为，不是故障。

### 转写出来的字是乱的 / 变成了别的语言

检查环境变量 `ASR_LANG`。说中文要填 `zh`，英文填 `en`。填错了它会硬往那个语言上凑，结果就很奇怪。

### 提示「太短啦，再说一次」

录音短于 0.5 秒会被直接拦掉。这是**故意**的保护——空白音频喂给语音识别，它会凭空「幻觉」出一些训练数据里的句子（比如「请点赞订阅」），拦掉比让它胡说好。

### 录错了一条，会不会污染基线

不会。点结果卡片上的 **✕**，那条记录会被删掉，**基线也会重新计算**，就像没听过一样。放心录。

### 部署成功了，但过一阵子服务自己挂了

先看是不是免费额度用完了（Groq 有月度限额）。另外如果你走的是「自己构建镜像」路线，**千万别用临时镜像仓库**（比如 ttl.sh，24 小时就失效），容器一重启就拉不到镜像了。用 Docker Hub 免费账号存。

> ### 🔒 安全上的三条底线
>
> - **密钥永远不要写进代码里**，只填在平台的环境变量里。代码是会被推到 GitHub 上的，写进去等于公开。
> - **密钥在聊天里贴过就算暴露了**。用完去平台把旧的作废、重新生成一个。
> - `data/` 目录里是你的声音数据，**那是隐私不是代码**。原项目的 `.gitignore` 已经挡住了它，别去改。

---

# 可选进阶

## 加上声纹锁和环境音识别

ears 还有两个没开的能力：

- **声纹锁** — 只有你的声音会被转写，别人说话只记「有人说话了」，内容不上云。这其实是个隐私保护功能。
- **环境音识别** — 认出雨声、猫叫、门铃、键盘声等 500 多种声音。

开启方法：把 `requirements.txt` 里的 `onnxruntime` 取消注释，再按原项目 README 的指引下载两个模型文件放进 `models/` 目录，重新部署。

> ### 💡 建议
>
> **先把基础版用几天再加这个。**
>
> 声纹锁需要另外录 6 段来注册你的声纹（它认的是声带特征，和音量语速基线是两套东西）。两个学习期撞在一起，你会分不清它到底在学什么。

## 如果平台不让你在线构建

那就自己在电脑上把镜像做好，让平台直接拉现成的。需要装 Docker Desktop，然后：

```bash
# 1. 注册 Docker Hub 免费账号，建一个 Read & Write 权限的 token
docker login -u 你的用户名

# 2. 在代码目录里构建并推上去
docker build -t 你的用户名/ears:v1 .
docker push 你的用户名/ears:v1

# 3. 回平台，把服务改成「从镜像部署」，填 你的用户名/ears:v1
```

之后每次改代码，重复第 2、3 步（换个新版本号）即可。

---

*这份指南基于一次真实的云端部署整理，上面每个「坑」都是实际卡住过、查明白之后写下来的。*

*ears 的原始项目与完整说明，见作者的 GitHub 仓库。*
